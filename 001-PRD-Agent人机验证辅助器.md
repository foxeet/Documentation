# PRD: Agent 人机验证辅助器

**编号**: 001
**日期**: 2026-05-28
**作者**: foxeet
**版本**: v0.1 (draft)
**标签**: #PRD #Agent #CAPTCHA #商业化 #VisionLLM

---

## 1. 产品概述

### 1.1 产品名称
Agent 人机验证辅助器(暂定,英文候选名:CaptchaPilot / VerifyAssist / HumanProxy)

### 1.2 一句话定位
专为 AI Agent 设计的人机验证(CAPTCHA)自动求解中间件 —— 当 OpenAI Operator、Anthropic Claude Computer Use、ChatGPT Agent 等浏览器/桌面操作型 Agent 在执行任务时遇到验证码,由本产品调用视觉大模型识别并返回操作指令(或直接代为完成鼠标操作),让 Agent 工作流不中断。

### 1.3 解决的核心痛点
当前以 OpenAI Operator、Anthropic Computer Use、ChatGPT Agent 等为代表的操作型 Agent,在完成订单、注册、查询、信息归集等任务时,几乎一定会被各类人机验证拦截:

- 极验 / 网易易盾 / 腾讯防水墙的滑块拖拽对齐
- Google reCAPTCHA / hCaptcha 的相似图片选择
- 文字点选(按顺序点击"春""夏""秋""冬")
- 旋转图片至正向
- 扭曲数字 / 字母识别
- 中文成语补全、空间方位推理

这些场景目前几乎 100% 会让 Agent 任务中断,需要人类介入。本产品要把这一步打通。

---

## 2. 背景与市场

### 2.1 行业趋势
- 2025 年起 Agent 浏览器/桌面操作能力指数级提升,任务长度从分钟级走向小时级
- 反爬/反 Bot 体系同步升级,几乎所有有价值的目标网站都接入了至少一层人机验证
- Agent 平台方(OpenAI、Anthropic)不会下场做"反反爬",留出第三方插件市场空间

### 2.2 竞品 / 现状
| 类型 | 代表 | 局限 |
|------|------|------|
| 人工打码平台 | 超级鹰、若快、2Captcha | 5-30 秒延迟、准确率受人员影响、复杂交互(滑块轨迹、旋转)支持差 |
| 专项算法 SaaS | yolo/CNN 滑块识别 | 针对单一厂商,泛化差,维护成本高 |
| 大模型 + 视觉 | 暂无垂直产品 | 通用模型方案存在,但没有针对 Agent 工作流封装的中间件 |

### 2.3 差异化
- 视觉大模型(豆包 vision 为主)统一识别多种验证类型,无需针对每家厂商单独训练
- 输出 Agent 可直接消费的"操作指令"(坐标、轨迹、点击序列),而非仅"识别结果"
- 提供轨迹模拟(贝塞尔曲线、人手加速度模拟),降低被风控判定为机器的概率

---

## 3. 目标用户

| 用户类型 | 场景 | 付费意愿 |
|----------|------|----------|
| Agent 开发者 / 创业团队 | 在自家 Agent 产品里集成,作为标准能力 | 高,按 API 调用付费 |
| RPA / 自动化运营团队 | 电商比价、票务、批量注册、数据归集 | 中高,按月订阅 |
| 个人极客 | 个人脚本、自用自动化 | 低,按次付费,需做合规筛查 |
| Agent 平台方(长期) | 直接接入成为基础设施 | 高,定制 / 私有化部署 |

---

## 4. 核心功能

### 4.1 验证类型覆盖(MVP 优先级)

| 优先级 | 类型 | 典型厂商 |
|--------|------|----------|
| P0 | 滑块拖拽(缺口对齐) | 极验 3/4 代、阿里、网易易盾 |
| P0 | 文字 / 字母 / 数字识别输入 | 自研、老式 CAPTCHA |
| P0 | 相似/语义图片点选 | reCAPTCHA v2、hCaptcha |
| P1 | 文字按序点选 | 极验、网易"请按顺序点击 X Y Z" |
| P1 | 旋转图片至正向 | 极验、字节自研 |
| P2 | 空间推理(哪个箭头朝上) | 腾讯防水墙 |
| P2 | 短视频/音频 CAPTCHA | reCAPTCHA audio |

### 4.2 核心能力模块

1. **截图采集**
   - 浏览器模式:扩展注入 content script 抓取验证码 iframe / canvas
   - 桌面模式:屏幕区域截图(用户或 Agent 框选,或自动检测)
   - API 模式:调用方上传截图
2. **类型识别 Router**
   - 先用一次轻量分类调用判断验证类型,再路由到对应 prompt
3. **视觉模型推理**
   - 主模型:**豆包 Vision**(实测精度优势)
   - 备选:Claude Opus 4.x Vision / GPT 系列 Vision / Qwen-VL(降级 / A-B 测试)
4. **操作指令生成**
   - 输出标准化 JSON,例如 `{action: "drag", from: [x,y], to: [x,y], trajectory: "bezier"}` / `{action: "click_sequence", points: [...]}` / `{action: "input", text: "..."}`
5. **执行层**
   - **SDK 模式**:返回指令给调用方 Agent 执行
   - **接管模式**(桌面插件):直接驱动系统鼠标键盘
   - **轨迹模拟**:贝塞尔 / Perlin 噪声、加速-减速曲线、随机抖动
6. **结果回灌**
   - 成功 / 失败回执 → 自动重试(最多 N 次)→ 失败兜底(切换模型 / 转人工众包)

### 4.3 调用方式示例

```http
POST /v1/solve
{
  "image_base64": "...",            // 或 image_url
  "context": {
    "page_url": "...",              // 可选,辅助判断厂商
    "captcha_type_hint": "slider"   // 可选,跳过分类节省 token
  },
  "options": {
    "trajectory": "human-like",
    "retry": 3
  }
}

→ 200
{
  "task_id": "tsk_xxx",
  "solved": true,
  "actions": [
    {"type": "drag", "from": [120, 88], "to": [263, 88], "duration_ms": 850, "path": "bezier"}
  ],
  "confidence": 0.93,
  "model": "doubao-vision-pro",
  "cost_credits": 2
}
```

---

## 5. 产品形态决策

三个方向待选,MVP 先做哪个需要决策:

### 方案 A:浏览器扩展(Chrome / Edge)
- ✅ 用户上手门槛低,可被自带浏览器的 Agent(本地版 Operator、Browser-Use 等)直接使用
- ✅ 易做 GTM(Chrome Web Store 商店分发)
- ❌ OpenAI Operator / Claude Computer Use 用的是云端虚拟浏览器,装不上扩展
- ❌ 部分网站对扩展注入有检测

### 方案 B:云 API + SDK(纯服务端)
- ✅ Agent 产品方最容易集成,符合 B2B 商业模式
- ✅ 不依赖客户端环境,兼容云端 Agent
- ❌ 需要调用方自己截图、自己执行操作
- ❌ 个人用户教育成本高

### 方案 C:桌面系统插件(全局接管鼠标)
- ✅ 适用于桌面 Agent / RPA 软件,能直接代操
- ✅ 商业价值高(企业 RPA 单价高)
- ❌ 研发成本最高,跨平台(Win / Mac / Linux)
- ❌ 安装感知差,需做代码签名 / Apple 公证

**建议路径**:**MVP = 方案 B(API)+ 浏览器扩展演示 demo**;桌面插件放到 v2。
理由:API 是最通用的商业化形态,所有产品形态都可以包它一层;浏览器扩展用来做获客和品牌曝光。

---

## 6. 技术架构(草图)

```
┌──────────────────────────────────────────────────┐
│  Client                                           │
│  (Agent SDK / 浏览器扩展 / 桌面插件)               │
└───────────────┬──────────────────────────────────┘
                │ HTTPS, API Key
┌───────────────▼──────────────────────────────────┐
│  API Gateway (鉴权、限流、计费、审计)              │
└───────────────┬──────────────────────────────────┘
                │
┌───────────────▼──────────────────────────────────┐
│  Solve Orchestrator                               │
│   ├── Type Classifier (轻量 vision call)          │
│   ├── Prompt Router                               │
│   ├── Doubao Vision (主) / Claude / GPT (备)      │
│   └── Action Generator + Trajectory Synthesizer   │
└───────────────┬──────────────────────────────────┘
                │
┌───────────────▼──────────────────────────────────┐
│  日志 / 训练数据回流 / 失败 case 人工审核管理后台   │
└──────────────────────────────────────────────────┘
```

---

## 7. 商业化方案

### 7.1 定价模型(初稿)

- **免费试用**:注册送 100 次调用
- **按量套餐**:¥0.05 - ¥0.20 / 次(按验证类型分档,滑块便宜、复杂图片贵)
- **包月订阅**:

  | 套餐 | 月费 | 调用数 | 并发 | 备注 |
  |------|------|--------|------|------|
  | Starter | ¥199 | 5,000 | 1 | 个人开发者 |
  | Pro | ¥999 | 30,000 | 5 | 小团队 |
  | Business | ¥4,999 | 200,000 | 20 | SLA 99.5% |

- **企业版**:私有化部署 / 定制模型,年度合同,起步 ¥10w+/年
- **OEM / API 转售**:给 Agent 平台或 RPA 厂商打白标,revenue share

### 7.2 官网核心模块

- Landing page(痛点 + demo 视频 + 一句话价值主张)
- 在线 **Playground**(贴一张验证码图,看实时解算结果 —— 转化核武器)
- 文档中心(API、SDK、扩展安装教程、最佳实践)
- 计费 / 控制台(API Key 管理、用量、账单、告警)
- 案例 / 客户 logo(初期可用匿名脱敏数据)
- 博客 / SEO(围绕 "AI Agent + CAPTCHA + Operator" 长尾词)
- 联系销售(企业版入口)

### 7.3 GTM 渠道

- Twitter / X / 即刻 在 Agent / RPA 社区投 demo 视频
- 渗透 Operator、Claude、Manus、扣子 等 Agent 用户社群
- 接入 LangChain / LlamaIndex / CrewAI / Browser-Use 等框架的 tools 生态
- Chrome Web Store 扩展引流
- 中文市场:扣子(Coze)、即梦、字节生态插件市场

---

## 8. 风险与合规 ⚠️

> 这一块是产品能不能存活的关键,必须前置想清楚。

### 8.1 法律 / 合规风险
- 验证码是 anti-bot 措施,绕过它在多数司法管辖区处于灰色地带
- 美国 CFAA、欧盟 GDPR / DSA、中国《反不正当竞争法》和《数据安全法》都可能适用
- **应对**:
  - 服务条款明确禁止用于"未经授权访问、爬取敏感数据、批量注册虚假账户"
  - 客户实名 + 用途登记 + KYC
  - 高敏目标(银行、政府、头部社交平台核心接口)进黑名单
  - 不主动宣传"破解 XX 网站"
  - 强调合规叙事:**用户授权自家 Agent 操作自家账户、企业内部 RPA**

### 8.2 风控对抗风险
- 验证码厂商会快速迭代,单一模型识别精度会随时间下滑
- **应对**:训练数据持续回灌 + 多模型集成 + 失败 case 人工标注循环

### 8.3 商业风险
- Agent 平台方(OpenAI / Anthropic / 字节)自己做这个能力,直接消灭中间层
- **应对**:做深(轨迹拟真、对抗风控)、做窄(垂直行业 RPA)、做快(品牌占位)

### 8.4 模型依赖风险
- 豆包 vision 服务变动 / 提价 / 接口下线
- **应对**:抽象 ModelProvider 接口,Claude / GPT / Qwen-VL 随时可切换

---

## 9. 路线图

| 阶段 | 时间 | 目标 |
|------|------|------|
| **P0 — 验证可行性** | Week 1-2 | 拿真实验证码图,用豆包 vision 跑通滑块 + 文字点选两类,精度 > 85% |
| **P1 — MVP API** | Week 3-6 | `/v1/solve` 上线,支持 3 类验证,鉴权 + 计费 + 控制台 |
| **P2 — 官网 + 公测** | Week 7-9 | 官网上线、Playground、文档、收 100 个种子用户 |
| **P3 — 浏览器扩展** | Week 10-12 | Chrome 扩展上架,获客漏斗 |
| **P4 — 商业版本** | Q2 | 包月套餐、企业版、SLA、私有化部署能力 |
| **P5 — 桌面插件** | Q3 | Windows 优先,Mac 跟进 |

---

## 10. 关键指标(成功标准)

- **技术**:整体识别成功率(P0 三类)≥ 90%,P95 延迟 ≤ 3s
- **业务 MVP**:3 个月内 ≥ 100 付费客户,MRR ≥ ¥3w
- **客户**:1 年内至少签下 3 家 Agent 平台或大型 RPA 客户做企业版

---

## 11. 待决问题

1. 产品名 / 域名
2. MVP 阶段先做浏览器扩展还是纯 API?(建议 API)
3. 豆包 vision 的定价、并发限制和稳定性能否撑住商业模式?需要用真实数据跑一轮压测
4. 是否要把"轨迹拟真"做成护城河,还是只做"识别"就够?
5. 海外市场 vs 国内市场优先级?(豆包合规性在海外是问号)
6. 是否要做"人工兜底"通道(类似超级鹰),用于无法识别时降级?
7. 失败 case 的图片数据用于训练,合规边界在哪里?需法务介入

---

*v0.1 — 初稿,待评审。*
